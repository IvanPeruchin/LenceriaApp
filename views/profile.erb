<!DOCTYPE html>
<html>
  <head>
    <title>Mi perfil</title>
    <link rel="stylesheet" href="/styles/profile.css" />
    <link rel="icon" href="/front/miniatura.png"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300&display=swap" rel="stylesheet">
  </head>
  <body>
    <header class="header">
      <a href=<%="/menu"%> class="volver">Volver</a>
    </header>
 
    <section class="settings">
        <h2 class="user">Nombre de usuario:  <%= @user.username %></h2>

        <h2 class="email">Email:  <%= @user.email %></h2>
        
        <% if @verificated %>
          <label>Validado</label>
        <% else %>       
          <label>No validado</label>
        <% end %>
    </section>
    <a href="/profile_change" class="btn"> Modificar datos </a>
  </body>
</html>